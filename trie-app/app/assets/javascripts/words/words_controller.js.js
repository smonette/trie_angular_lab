// Generated by CoffeeScript 1.8.0
var MyWordsController, WordCtrls;

WordCtrls = angular.module("WordCtrls", []);

MyWordsController = (function() {
  function MyWordsController(scope, Word) {
    this.scope = scope;
    this.Word = Word;
    this.greeting = "These are words!";
  }

  MyWordsController.prototype.addWord = function(newWord) {
    return this.Word.create(newWord).success(function(data) {
      return console.log(data);
    });
  };

  return MyWordsController;

})();

WordCtrls.controller("MyWordsController", ["$scope", "Word", MyWordsController]);
