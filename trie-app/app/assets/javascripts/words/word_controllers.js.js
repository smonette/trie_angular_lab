// Generated by CoffeeScript 1.8.0
var MyWordsController, WordCtrls;

WordCtrls = angular.module("WordCtrls", []);

MyWordsController = (function() {
  function MyWordsController(scope, http, Word) {
    this.scope = scope;
    this.http = http;
    this.Word = Word;
    this.greeting = "These are words!";
    this.Word.all().success((function(_this) {
      return function(data) {
        return _this.words = data;
      };
    })(this));
  }

  MyWordsController.prototype.addWord = function(newWord) {
    return this.Word.create(newWord).success((function(_this) {
      return function(data) {
        return _this.words.push(data);
      };
    })(this));
  };

  return MyWordsController;

})();

WordCtrls.controller("MyWordsController", ["$scope", "$http", "Word", MyWordsController]);
